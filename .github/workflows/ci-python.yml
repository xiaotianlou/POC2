name: ci-python (minimal)

on:
  push: { branches: [main] }
  pull_request: { branches: [main] }
  workflow_dispatch: {}

jobs:
  ci:
    uses: xiaotianlou/gha-templates/.github/workflows/python-ci-min.yml@v2
